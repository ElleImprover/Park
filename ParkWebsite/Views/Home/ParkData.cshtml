@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}

<h1>Parks</h1>
<br />
@*<table>

        <tr>
         @Model.Park.Parkname
        </tr>
        <tr>
            Borough: @Model.Park.Borough
        </tr>
        <tr>
            Acres: @Model.Park.Acres
        </tr>
        <tr>
            Description: @Model.Park.Description
        </tr>
    </table>*@
@if (ViewBag.Data != null)
{
    <table class="table">
        @*<thead>
                <tr>
                    @foreach (var property in Model.EntityProperties)
                    {
                        <th><h1>@System.Globalization.CultureInfo.CurrentCulture.TextInfo.ToTitleCase(property)</h1></th>

                    }
                </tr>
            </thead>
            <tbody>*@

        @foreach (var record in ViewBag.Data)
        {
            <tr>
            <tr>@record[@Model.Park.Parkname]</tr>
            <tr>@record[@Model.Park.Borough]</tr>
            <tr>@record[@Model.Park.Acres]</tr>
            <tr>@record[@Model.Park.Description]</tr>
            @*@foreach (var property in Model.EntityProperties)
                {
                    if (@property.Contains("id"))
                    {
                        <td><a href="/entity/@Model.EntityName/@record[@property]">@record[@property]</a></td>
                    }
                    else
                    {
                        <td>@record[@Model.Park.Parkname]</td>
                    }
                }*@
            </tr>
        }
        @*</tbody>*@
    </table>
}